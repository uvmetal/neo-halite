import React from 'react';
import { Link } from 'react-router-dom';
import NavBar from './components/navbar'

const Accounts = () => (
  <div>
    <NavBar />
    <div class="container">
    <h2 class="text-muted">Accounts</h2>
  </div>
  </div>
);

export default Accounts;
